请基于当前已经实现 Difference Query 的 ZR-0 仓库继续修改，要求最小改动。不要重新实现或覆盖现有 Query、SDPA、mRoPE、checkpoint 和 direct-action 路径；先审查当前实现，再补齐 Query-conditioned teacher-forced AR 训练。

目标：
1. 第一阶段只训练 VLM + Difference Query，自回归预测专家文本，只使用 AR loss。
2. 联合阶段同时使用 Query-conditioned AR loss 和 Flow Matching action loss。
3. Action Expert 在所有 Query 模式下仍只能读取 final RMSNorm 后的 Query hidden states。
4. 本阶段不实现 Query 模式的自由生成或 KV-cache；`.generate()` / `get_action_subtask()` 继续在调用前抛 NotImplementedError。

先审查并报告：
- 当前 Query forward 是否已经支持 labels、teacher-forced LM loss 和 [C,Q,T,P] label 重排。
- 当前是否仅“不支持 .generate()”，还是连 teacher-forced AR loss 也不支持。
- 当前 action-only 路径是否仍无意计算 LM loss。
- 当前数据 loader、dataset2feature 配置和 checkpoint 恢复逻辑。
- 不要仅凭函数存在就判断支持，必须用测试验证梯度和 token 对齐。

训练接口优先复用现有参数，不增加重复布尔开关：
- loss_type=vlm：AR-only。
- loss_type=action：Flow-Matching-only。
- loss_type=vlm_and_action：joint。
- vlm_loss_weight：lambda_AR。
- action_expert_loss_weight：lambda_FM。
总损失必须由外部参数计算：
loss = lambda_AR * ar_loss + lambda_FM * flow_matching_loss。
两个权重必须有限且非负；joint 模式要求二者均显式传入且大于0。保留旧 CLI/checkpoint 兼容性，并记录 raw loss、weighted loss、监督文本 token 数量和当前权重。

执行约束：
- AR-only 必须跳过 Action Expert 的 prepare_inputs、forward、噪声采样和 Flow loss；Action Expert 无梯度。
- action-only 不应计算无用的 LM loss。
- joint 才执行两个分支。
- Query + action/joint 与 detach_vlm_outputs_for_action_expert=True 冲突时明确报错。
- 不修改 DiT、Flow Matching、state/action encoder 或 decoder。

Query AR mask 保持：
C→C causal；Q→C 全可见；Q→Q 双向；T→Q 全可见；T→T causal；T 不可读取 C；任何有效 token 不读取 P。
“T 只能看到 Query”是指文本的外部条件只能来自 Query；T 必须能读取之前的 T token，否则不构成自回归模型。

Query 模式下必须重排 input embeddings、attention mask、position_ids 和 labels：
[C,Q,T,P]
labels=[-100_C,-100_Q,target_token_ids_T,-100_P]。
使用 Qwen 原生 causal-LM loss，不手写另一套 CE，除非当前 Transformers 接口确实无法使用。
必须测试第一个 T label 由最后一个 Q 位置的 logit 预测；Query placeholder、C 和 P 绝不能进入 AR loss。
若样本没有有效 target，或截断后监督 token 数为0，必须明确报错或计数后丢弃，不能产生 NaN 或静默零损失。

检查数据集：
/opt/data/private/lq/datasets/molmoact_dataset_tabletop-v3_stage05
先只读检查目录布局、parquet/schema、split、所有字段类型、图像存储方式，并抽查1至2条样本。专家文本字段名为 train_data。
当前 loader 若固定读取 json，则将文本字段做成 dataset2feature.yaml 的可配置项，如 text_field: train_data；不要在通用 loader 中硬编码该路径或字段。
确认 train_data 是完整 conversation JSON、结构化对象还是单独 assistant 答案，然后按真实 schema 实现最小适配。禁止猜测字段。
若是纯 VLM 数据，AR-only 不要创建随机 dummy action；action/joint 模式使用该数据时必须因缺少真实 state/action/stats 而 fail fast。
若路径在当前环境不可访问，明确报告，不要声称已经验证，并使用合成样本完成模型测试。

归一化审查：
- 追踪训练时 state/action 的 stats 来源、q01/q99 或 min/max 选择、padding 和 action_mask。
- 追踪推理时裁维及 min_max_denorm。
- AR-only 不执行 action/state 归一化。
- 用真实 VLA 样本增加 normalize→denormalize round-trip 测试；不要修改既有归一化公式。

至少增加以下测试：
- Query 关闭时原始训练和推理回归不变。
- [C,Q,T,P] mask 和 labels 每个 block 正确。
- 修改 T 不改变 Query hidden；修改 C 会改变 Query hidden。
- 未来 T 不影响较早文本 logits。
- AR-only：AR loss 有限非零，Query/VLM 有梯度，Action Expert 无梯度且未 forward。
- action-only：不计算 AR loss，Action Expert 只收到 [B,Nq,H]。
- joint：两个 raw/weighted loss 正确相加，Query 和 Action Expert 均得到预期梯度。
- train_data 解码后的监督 labels 与原始专家答案一致。
- 实际数据一个 dataloader batch完成 forward/backward；不要启动正式长训练。
- checkpoint 保存加载后 Query、训练接口和已有 direct-action 行为一致。

提供三个 dry-run 命令：
1. AR-only：loss_type=vlm，lambda_AR=1，lambda_FM=0，不启用 tune_action_expert。
2. action-only：loss_type=action，lambda_AR=0，lambda_FM=1。
3. joint：loss_type=vlm_and_action，两个权重均从 CLI 显式传入。
联合权重先只提供占位符和建议 sweep，不硬编码最终论文值。

完成后报告：修改文件、当前 teacher-forced AR/.generate() 支持矩阵、数据 schema 结论、归一化链路、测试命令及结果、未执行事项。保留用户已有修改，不创建 commit，不启动正式训练。