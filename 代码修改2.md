这是一个新的功能开发任务。当前工作树中已经完成并修复的 Difference Query 实现（已经commit）是本任务的基线和事实来源。

开始前请先执行 git status、检查当前 diff 和相关测试，理解现有实现后再修改。不要回退、覆盖或重新实现已有 Difference Query 功能；不要处理无关的已暂存、未暂存或未跟踪文件。

上一轮 checkpoint 双目录 sidecar 一致性问题必须先确认已经修复并通过测试；如果尚未修复，先完成该修复，再开始本任务。

请基于当前已经实现 Difference Query 的 ZR-0 仓库继续修改，严格遵循“先审计、后最小修改、再验证”的顺序。不要重构无关代码，不覆盖用户已有修改，不创建 commit，也不要启动正式长时间训练。

目标：
1. 第一阶段支持 Query-conditioned teacher-forced AR 文本训练，只计算 AR loss。
2. 后续支持 AR loss 与 Flow Matching action loss 联合训练。
3. 专家文本来自：
   /opt/data/private/lq/datasets/molmoact_dataset_tabletop-v3_stage05
   目标字段为 train_data。
4. 动作专家始终只能读取 final RMSNorm 后的 Difference Query hidden states。

一、先审计当前实现

请检查并给出代码位置和结论：

- Query 模式是否支持带 labels 的 `[C,Q,T,P]` teacher-forced causal LM loss。
- 重排后的 labels 是否只在 T 位置有效，C/Q/P 是否为 -100。
- HF causal shift 后，第一个 T token 是否由最后一个 Query 位置预测。
- T 是否只能看到 Q 和先前 T，不能直接看到 C、P 或未来 T。
- Q 是否完全看不到 T，确保动作分支不会泄漏专家文本。
- 当前 `loss_type=vlm/action/vlm_and_action` 和两个 loss weight 是否已正确实现。
- Query 模式下 `.generate()` 是否仍抛出 NotImplementedError。保留该限制，本任务不实现推理期文本生成。
- 最后一轮 checkpoint 双目录 sidecar 一致性 P2 是否已经修复。

如果上述模型能力已经存在，请复用，不要添加重复布尔开关。

二、训练接口

优先继续使用现有接口：

- `--loss_type vlm`：AR-only。
- `--loss_type action`：Flow Matching only。
- `--loss_type vlm_and_action`：联合训练。
- `--vlm_loss_weight`：AR loss 系数 λ_AR。
- `--action_expert_loss_weight`：Flow Matching 系数 λ_FM。

联合目标必须严格为：

`total_loss = λ_AR * ar_loss + λ_FM * flow_matching_loss`

校验权重为有限、非负数，联合模式下不能同时为 0。日志同时记录 raw loss、weighted loss、total loss 和两个权重。可增加 `ar_loss` 日志别名，但保留现有 `vlm_loss` 键兼容旧脚本。

AR-only 模式必须提前返回，不调用 Action Expert；Action Expert 参数无梯度。Query 和配置为可训练的 VLM 参数必须获得有限非零梯度。

三、数据集兼容性

只读检查目标目录的文件布局、schema 和少量样本，不要扫描或加载整个数据集。确认：

- 是 JSONL、Parquet、LeRobot，还是需要关联源数据的派生标注。
- `train_data` 的实际类型、字段顺序和缺失率。
- 图像、任务、state、action 是否直接存在；若只有 source URI，验证能否定位源数据。
- `training_eligible` 的数量分布。
- 标注时间区间与 Flow Matching action chunk 是否对齐。

不要假定当前 VQADataset 可以读取它。若不兼容，增加职责单一的 dataset adapter，并在 dataset 配置中显式声明 target field 为 `train_data`。

若 `train_data` 是字典，固定按：
`Task_temporal, Spatial_motion, Contact_interaction, Object_constraints`
序列化为确定性 JSON；若是字符串则原样使用。其他类型或缺失字段应给出包含 sample id 的明确错误。

构造 Qwen chat 数据时：
- system/user 部分只能含当前可用观测、任务和允许的本体状态。
- assistant content 为 train_data。
- 不得把未来图像、未来状态、future timestamp、slot_data 或 train_data 泄漏进输入 C。
- 检查 truncation，统计有效监督 token 数和 target 被截断比例。
- 没有有效 T token 的样本必须过滤或明确报错，不能产生全 -100 的 NaN loss。

四、归一化要求

请追踪并记录当前 ZR-0 的完整路径：

raw state/action
→ 使用哪份 stats、min/max 或 quantile 归一化
→ padding 与 mask
→ Flow Matching
→ action decoder
→ 推理期反归一化

AR-only 模式不需要 state/action 归一化，也不能依赖 FAST tokenizer 或 dummy action 才能读取数据。

联合模式必须使用真实 state/action 和原 ZR-0 归一化逻辑。缺少真实动作或统计量时直接报错，不允许 dummy action。train_data 中的文本和数字不做数值归一化。

五、验证

至少增加以下测试：

- labels 重排、causal shift 和 C/Q/T/P 全部可见性。
- AR loss 有限非零，Query/VLM 有梯度，Action Expert 无梯度。
- 联合 loss 与手工加权和完全一致，两个分支均有正确梯度。
- AR-only batch 不含 action/state 时仍可训练。
- 联合 batch 缺少 action/state/stats 时立即失败。
- 修改 T 不改变 Query hidden；修改 C 会改变 Query hidden。
- 解码 supervised labels 后与规范化的 train_data 完全一致。
- 归一化→反归一化在有效动作维度上 round-trip。
- Query action-only 和 Query-off 原有测试保持通过。

若资源允许，使用目标数据集完成：
1. dataset 单样本读取；
2. collate 一个 batch；
3. tiny 模型 AR-only forward/backward；
4. 真实 Qwen3-VL-2B 的单 batch AR-only smoke；
5. 若数据具有对齐的真实动作，再执行单 batch joint smoke。

不要启动正式训练。若数据缺少图像、动作、统计量或可靠 join key，请停止在可验证边界，准确说明 AR-only 和 joint 分别被什么阻塞，不要伪造 dummy 输入。

最终交付：
- 当前能力审计结论。
- 修改文件及原因。
- 数据 schema、样本量、eligible 比例和兼容性结论。
- AR-only 与 joint 的完整启动命令。
- 测试命令和实际结果。
- 尚未验证的风险，尤其是目标文本与 action horizon 的时序对齐。