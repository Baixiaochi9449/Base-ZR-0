请基于当前工作树中已经实现的 Difference Query、Query-conditioned AR、Optical Flow Head 和三阶段训练框架，继续做最小改动，实现 `Structured Slot Heads v1`。

本任务的设计决策已经确定。请先检查当前代码和真实数据，确认接入位置及数据合同，然后直接完成实现、测试和文档。除非真实代码或数据与下述规格存在无法兼容的冲突，否则不要重新询问设计选择。

# 一、总体目标

新增一个可插拔、仅训练期使用的 Slot Auxiliary 模块，用 `slot_data` 对前部 Difference Query 施加 Q1–Q9 结构化监督。

约束：

- Difference Query 是总开关。
- 只有 `use_difference_query=True` 时才能启用 Slot Head。
- Slot Head 与 Optical Flow Head 可以独立开启或关闭。
- Slot Head 关闭时不得实例化模块、加入 optimizer 或计算 Slot loss。
- Slot Head 仅用于训练和分析。
- direct-action、server、普通推理不读取 `slot_data`，不运行 Slot Head，不增加推理开销。
- Action Expert 继续读取全部 Difference Query，不能读取 Slot Head 的预测结果。
- 保留当前 Query 之间的双向注意力，不修改现有 Qwen block mask。
- Slot/Flow 分组是直接监督位置的软分工，不要求组间梯度完全隔离。

# 二、先审计真实代码和数据

首先检查：

1. 当前 Difference Query、Optical Flow Head、loss mode、checkpoint、dataset adapter、DDP reduction 和 optimizer 构造方式。
2. 优先复用现有 auxiliary-head registry、配置解析、checkpoint 和缺失标签处理，不建立第二套重复机制。
3. 检查真实数据：

```text
/opt/data/private/lq/datasets/molmoact_dataset_tabletop-v3_stage05
```

重点审计 `slot_data`：

- 是 JSON 字符串还是已经解析的对象；
- schema/version；
- Q1–Q9 字段覆盖率、null、非法值和有效 mask 分布；
- Q2/Q7/Q8 的完整类别集合和频次；
- bbox、contact point 对应哪一路相机；
- bbox 是否为归一化 xyxy、是否越界或端点倒置；
- Q5 的单位、统计范围，以及是否满足
  `relative_displacement = gripper_displacement - target_displacement`；
- Q9 是否按 risk 降序排列，零 padding 和 `valid_mask=0` 的准确语义；
- `t -> tK` 的 frame/time interval 分布。

不能根据示例猜测相机、类别词表或位移符号。若无法从数据或生成配置确认空间标注对应的相机，应停止启用 Q3/Q4/Q6/Q9 空间损失并明确报告。

输出一份只读数据审计结果，类别词表和归一化统计只能使用训练 split 计算。

# 三、接口设计

在现有接口风格上增加：

```text
slot_aux_type:
- none
- structured_slots_v1
```

同时支持外部传入：

```text
slot_loss_weight
slot_task_weights
slot_schema_version
```

默认：

```text
slot_schema_version = future_difference_training_v5

Q1=.20 Q2=.20 Q3=.04 Q4=.04 Q5=.04
Q6=.20 Q7=.12 Q8=.12 Q9=.04
```

校验：

- `slot_aux_type != none` 时必须启用 Difference Query。
- Slot Head 与 Optical Flow Head 独立开关。
- `num_flow_queries` 是稳定的 Query 角色布局配置，即使 Flow Head 关闭，也不能自动把尾部 Flow Query 重新分给 Slot。
- `num_slot_queries = num_difference_queries - num_flow_queries`。
- 启用 Slot Head 时要求 `num_slot_queries >= 7`。
- checkpoint 中的角色分区优先；显式 CLI 与 checkpoint 冲突时报错。
- 所有 loss 系数由配置或 CLI 传入，不在训练逻辑中写死。

# 四、Query 动态分组

前 `num_slot_queries` 个 Query 为 Slot Query，后 `num_flow_queries` 个为 Flow Query。

把 Slot Query 按以下七个连续语义组分配：

```text
G12：Q1 progress + Q2 phase
G3 ：target bbox
G4 ：gripper bbox
G5 ：target/gripper/relative displacement
G6 ：affordance contact points
G78：Q7 gripper transition + Q8 contact transition
G9 ：obstacles
```

基准比例：

```text
[2, 2, 2, 3, 2, 2, 3]
```

动态分配算法必须确定且可测试：

1. 七组各分配一个 token；
2. 剩余 token 按上述比例分配；
3. 使用最大余数法；
4. 余数相同时按固定组顺序处理；
5. 所有 Slot Query 必须恰好属于一个组，不得遗漏或重叠。

每组对自己的 Query hidden states 做无参数均值池化，再送入相应 Head。Head 输入使用 final RMSNorm 后的 Query hidden states。

# 五、Slot Head 架构

只使用轻量 `LayerNorm + Linear`：

```text
Q1: [B,H] -> [B,2]
Q2: [B,H] -> [B,2,7]
Q3: [B,H] -> [B,2,4]
Q4: [B,H] -> [B,2,4]
Q5: [B,H] -> [B,3,3]
Q6: [B,H] -> [B,2,2]
Q7: [B,H] -> [B,4]
Q8: [B,H] -> [B,4]
Q9: [B,H] -> [B,3,6]
```

共享规则：

- Q1/Q2 只共享 G12 输入，不共享 LN 或 Linear。
- Q3/Q4 不共享参数。
- Q5/Q6 不共享参数。
- Q7/Q8 共享 G78 输入和 LayerNorm，不共享分类 Linear。
- Q9 三个有序障碍物位置复用同一个输出 Head。

输出约束：

- Q1、Q6 和 Q9 risk 使用 sigmoid。
- Q2/Q7/Q8 输出 logits。
- Q3/Q4/Q9 bbox 使用合法参数化：

```text
x1 = sigmoid(ax)
y1 = sigmoid(ay)
x2 = x1 + (1-x1) * sigmoid(aw)
y2 = y1 + (1-y1) * sigmoid(ah)
```

对 `H=2048`，按上述结构预计参数量为 `178,247`。实现后必须使用 `sum(p.numel())` 报告准确参数量，并测试它不随 Slot Query 数变化。

使用隔离 RNG 初始化，不能改变 Action Expert 或其他已有模块的随机初始化结果。

# 六、Slot 数据张量合同

Adapter/collator 将 `slot_data` 规范化为固定张量和独立 mask，例如：

```text
Q1 values [2], mask [2]
Q2 labels [2], mask [2]
Q3 bbox [2,4], mask [2]
Q4 bbox [2,4], mask [2]
Q5 displacement [3,3], mask [3]
Q6 points [2,2], mask [2]
Q7 label scalar, mask scalar
Q8 label scalar, mask scalar
Q9 bbox [3,4], risk [3], presence_target [3],
   presence_supervision_mask [3]
```

要求：

- `slot_data` 只能作为监督标签，不能进入 prompt、`train_data` 或 VLM context。
- 缺失、null、非有限值和无效 mask 不进入 loss。
- 未知分类字符串必须携带 dataset/episode/frame 信息报错，不能静默映射。
- Q9 中显式 `valid_mask=0` 是 presence 负样本；整个 mask 字段缺失时不能自动制造负标签。
- 保留缺失标签状态，不能把缺失数值补零后当作有效监督。

# 七、损失函数

定义：

```text
L_slot_raw = Σ alpha_i * L_Qi
L_slot_weighted = slot_loss_weight * L_slot_raw
```

每个 Q 的 loss：

- 先对单样本有效元素求 masked mean；
- 再按跨 rank 的全局有效样本数求均值；
- 不按原始 batch size 平均；
- 某项缺失时不重新归一化其他 Q 的权重。

具体损失：

```text
Q1: Smooth L1
    + 0.1 * relu(progress_t - progress_tK)
    单调项仅在两个端点都有效时计算

Q2: fixed class-weighted CE
    + label smoothing 0.02

Q3/Q4: Smooth L1
       + 0.1 * (1 - GIoU)

Q5: 使用训练 split 固定 mean/std 的 normalized Smooth L1
    + 0.05 * 米制一致性损失
    consistency = relative - (gripper - target)
    一致性项仅在三个向量都有效时计算

Q6: Smooth L1

Q7/Q8: fixed class-weighted CE
       + label smoothing 0.02

Q9: 0.5 * presence BCE
    + 0.25 * bbox loss
    + 0.25 * risk Smooth L1
```

Q2/Q7/Q8：

- 从完整训练 split 的有效标签统计固定类别词表和频次；
- 使用平方根逆频率；
- 限制最大类别权重比；
- 将平均样本权重归一化为 1；
- 保存类别词表、频次、权重和生成规则；
- 不允许按 mini-batch 动态计算权重。

Q5 标准差必须设置数值下限，避免除零。

所有 masked loss 和 reduction 使用 FP32。

# 八、缺失监督和分布式行为

Stage 2：

- 默认只采样至少包含一个有效 Slot 标签的样本；
- Slot 和 Flow loss 独立计算；
- 有 Slot、无 Flow 时，Flow loss 为 graph-connected zero；
- 有 Flow、无对应 Slot 子项时，只计算有效 Slot 子项；
- 全局 batch 同时没有 Slot 和 Flow 监督时，所有 rank 同步跳过 backward、optimizer、scheduler 和 global step；
- 不能执行零 loss 的 AdamW step，因为 weight decay 仍会更新参数。

Stage 3：

- 样本没有 `slot_data` 时，Slot loss 为 graph-connected zero；
- 该样本仍可参与 AR、Flow和FM；
- 不能因为 Slot 缺失而跳过整个优化步。

某个 rank 没有有效 Slot、其他 rank 有时：

- 本 rank 返回连接 Slot Query 和所有 Slot Head 参数的可微零；
- 使用全局有效计数正确归一化；
- 考虑 DDP 对梯度取平均的行为，确保结果等价于单机全局 batch mean；
- 不依赖 `find_unused_parameters=True` 掩盖错误。

# 九、三阶段与 checkpoint

保持以下语义：

```text
Stage 1:
L = lambda_AR * L_AR

Stage 2:
L = lambda_slot * L_slot + lambda_OF * L_OF

Stage 3:
L = lambda_AR * L_AR
  + lambda_slot * L_slot
  + lambda_OF * L_OF
  + lambda_FM * L_FM
```

Stage 2：

- 使用 `init_from_checkpoint` 加载已有 Stage 1 的 VLM 和 Difference Query。
- Stage 1 checkpoint 没有 Slot/Flow Head 权重是合法情况。
- Slot/Flow Heads 使用隔离 RNG 随机初始化。
- 默认冻结 VLM 和 Action Expert。
- 只训练 Difference Query、Slot Head和启用的Flow Head。
- 冻结 VLM 仅设置参数 `requires_grad=False`；不能对 VLM forward 使用 `no_grad`、`inference_mode` 或 detach，因为 action/aux loss 仍需穿过冻结 VLM 向 Difference Query 反传。

Stage 3：

- 从 Stage 2 checkpoint 加载 VLM、Query、Slot Head和Flow Head。
- Action Expert 默认按 ZR-0 流程和固定 seed 随机初始化。
- 除非显式提供独立 Action Expert checkpoint，否则不能从 Stage 1/2 checkpoint 隐式复用旧 Action Expert。
- 默认训练 VLM、Query、辅助 Heads和Action Expert。

必须继续区分：

- `init_from_checkpoint`：跨阶段加载模型参数，optimizer、scheduler、global step重新开始；
- `resume_from_checkpoint`：同阶段断点续训，恢复模型、optimizer、scheduler、RNG、sampler和global step。

新增并保存：

```text
slot_aux_config.json
slot_head.safetensors
slot_supervision_stats.json
```

其中保存：

- schema version；
- Query 分区与七组边界；
- 分类词表、频次和权重；
- Q5 mean/std；
- loss 配置和内部权重；
- hidden size及权重 shape。

配置存在但权重缺失、权重存在但配置缺失、shape不符、词表不符或Query分区冲突都必须报错。

# 十、多时域语义

第一版保留两种不同未来时域：

- Slot：当前状态 `t` 到语义关键时刻 `tK`；
- Optical Flow：当前帧 `t` 到固定 `t+10`。

不要筛选成同一时域，不添加跨 Head 一致性损失，也不新增 horizon conditioning。日志和文档必须明确这是互补的多时域监督，不能将二者描述成同一个未来状态。

# 十一、日志与评估指标

至少记录：

```text
slot_loss_raw
slot_loss_weighted
slot_loss_computed
slot_sample_coverage
每个 Q 的 valid count、raw loss和weighted loss
Q2/Q7/Q8 accuracy 与 macro-F1
Q3/Q4 bbox L1 与 GIoU
Q5 米制 RMSE 与 consistency error
Q6 point L1
Q9 presence precision/recall/F1、bbox和risk误差
```

Slot Head 未启用或当前 batch 无有效监督时，要如实记录状态，不能输出误导性的正常 loss。

允许增加显式 validation/debug API 输出 Slot 预测，但不能挂入默认控制推理路径。

# 十二、测试要求

先补测试，再完成实现。至少覆盖：

1. Query 分组在 `N_slot=7/16/20` 等情况下非空、无重叠、无遗漏且确定性一致。
2. Slot Head 直接输入只来自前部 Slot Query。
3. Head 参数量小于 0.2M，且不随 `N_slot` 改变。
4. Q1–Q9 输出 shape、范围和合法 bbox。
5. 每项 loss 与手算结果一致。
6. null、NaN、非法 mask、未知类别、Q9 显式负样本语义。
7. 单个 Q 缴失、整条样本缺失、单 rank 缺失和全局缺失。
8. DDP reduction 等价于单机全局 masked mean。
9. Stage 2 冻结 VLM 后，Query 和 Heads 获得有限非零梯度，VLM与Action Expert不更新。
10. Stage 1 → Stage 2 初始化不会错误恢复 optimizer/global step。
11. Stage 2 → Stage 3 正确加载 Aux Heads并重新初始化Action Expert。
12. Slot关闭路径与修改前行为回归一致。
13. direct-action/server不读取Slot标签、不运行Slot Head。
14. checkpoint保存加载后配置、统计量、权重和固定输入输出一致。
15. 真实数据完成单 batch和1–2 optimizer-step smoke test，检查loss、梯度和参数更新均无NaN/Inf。

运行相关单测、CLI help、launcher dry-run和`git diff --check`。不要启动正式大规模训练或完整benchmark评估。

# 十三、工程约束与最终交付

- 最小化修改范围，复用当前 registry、adapter、loss registry和checkpoint机制。
- 保持现有 Query-off、AR-only、Flow-only、action-only和direct-action路径兼容。
- 不修改无关代码，不覆盖用户已有修改，不处理用户笔记文件。
- 不创建commit。
- 若工作树已有无关修改，保留并明确区分。
- 最终只暂存本任务产生的代码、测试和文档；若无法可靠区分，则不要暂存并说明原因。

完成后汇报：

1. 实际修改的文件和核心行为；
2. 真实数据审计结果；
3. 最终 Slot Head 准确参数量；
4. 实际运行的测试及结果；
5. 未运行的测试和原因；
6. Stage 1 → Stage 2、Stage 2 → Stage 3 的完整启动命令；
7. 当前仍存在的风险或未验证事项。

若真实数据合同与本文规格不一致，请先提供具体字段、样本和代码证据，只暂停受影响的部分，不要静默猜测或扩大修改范围。