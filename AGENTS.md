# AGENTS.md

## 1. 适用范围

本文件适用于整个仓库。修改任何代码、配置、脚本或文档时，都必须遵守以下约束。

如果用户在当前任务中给出了更明确的要求，以用户当前任务的明确要求为准。

---

## 2. Git 与暂存区规则
完成修改和必要验证后，将本次任务产生且应由 Git 跟踪的文件放入暂存区，包括：
   - 修改过的源代码；
   - 新增的源代码；
   - 新增或修改的配置文件；
   - 新增或修改的测试；
   - `reference.md`；
   - 实验说明文档。

不要执行以下操作，除非用户明确要求：
   - `git commit`
   - `git commit --amend`
   - 创建 Git tag
   - `git push`
   - 回退或丢弃用户已有修改

---

## 3. 代码修改原则

### 3.1 优先复用现有实现

修改或新增功能前，先搜索仓库中是否已经存在可以复用的：

- 模型模块；
- 数据处理函数；
- 配置系统；
- 注册机制；
- 损失函数；
- 训练循环；
- 日志与评估工具；
- 公共辅助函数。

优先复用或扩展现有实现，避免重复实现相同逻辑。

不得为了方便复制出多份只有少量差异的代码。确实需要分支实现时，应提取公共逻辑或使用配置控制差异。

### 3.2 大幅修改文件

如果某个文件已经过大，或者新功能会导致文件职责混乱，可以新建独立模块，并在原文件中进行最小化接入。

新建文件时必须满足：

- 文件职责明确；
- 接口清晰；
- 已连接到实际调用路径；
- 不要留下未使用的新文件；
- 不要简单创建 `xxx_new.py`、`xxx_v2.py` 等重复版本来替代原文件；
- 除非用户明确要求，不得删除原实现。

### 3.3 新功能必须可关闭

新增模块、训练目标或模型设计时，原则上必须提供显式开关，例如：

- 配置文件字段；
- 命令行参数；
- Feature flag；
- 模块注册选项。

开关应集中管理，避免在不同文件中散落多个不一致的布尔变量。

在未明确要求改变默认行为时：

- 新功能默认关闭；
- 关闭新功能后，数据流、模型结构、loss 和训练行为应保持原有逻辑；
- 不应额外加载无用模型、数据或依赖；
- 不应产生额外 loss；
- 不应改变原模型输入输出；
- 不应要求已有配置必须增加新字段。

新增配置字段必须提供向后兼容的默认值。

### 3.4 禁止静默改变行为

以下变化必须显式记录，不得静默发生：

- 输入图像尺寸或预处理发生变化；
- 数据采样方式发生变化；
- 可训练或冻结模块发生变化；
- loss 组成或权重发生变化；
- optimizer、scheduler 或学习率发生变化；
- action chunk、horizon 或执行步数发生变化；
- checkpoint 加载范围发生变化；
- 模型默认配置发生变化。

---

## 4. `reference.md` 维护要求

仓库根目录必须存在 `reference.md`。如果不存在，应创建并放入暂存区。

每次修改代码、新增模块或改变训练行为后，都必须同步更新 `reference.md`。

### 4.1 每项修改需要记录的内容

每项设计至少记录：

1. 修改名称和目的；
2. 涉及的文件、类和函数；
3. 该实现属于以下哪一种：
   - 直接复用仓库原有模块；
   - 基于仓库原有模块修改；
   - 参考外部项目或论文实现；
   - 完全新增的自定义实现；
4. 如果复用仓库代码：
   - 写明原始文件路径；
   - 写明原始类或函数名称；
   - 说明是否进行了适配；
5. 如果参考外部实现：
   - 写明论文、项目或代码仓库名称；
   - 提供链接、版本或 commit；
   - 说明借鉴了什么，而不是笼统写“参考某项目”；
6. 如果是自定义新增：
   - 说明模块结构；
   - 说明输入和输出；
   - 说明数据流；
   - 说明设计目的；
   - 说明为什么现有实现无法直接满足需求；
7. 新功能对应的开关、默认值以及关闭后的行为；
8. 与原有逻辑的兼容性；
9. 使用什么测试或检查验证修改。

### 4.2 推荐记录格式

```md
## 修改名称

- 日期：
- 修改目的：
- 涉及文件：
- 配置开关：
- 默认状态：
- 实现来源：原仓库复用 / 原仓库适配 / 外部参考 / 自定义新增
- 原始实现位置或外部来源：
- 具体设计：
- 输入与输出：
- 与原有流程的关系：
- 关闭功能后的行为：
- 验证方式：
- 已知限制：
```

`reference.md` 应描述有效的当前设计。过时内容需要标记为废弃或同步更新，不能让文档与实际代码相互矛盾。

---

## 5. 训练任务执行约束

### 5.1 启动权限

只有用户明确要求启动训练时，才能实际运行训练任务。

代码修改、训练方案设计或配置检查，不等于授权启动训练。不得因为完成了训练配置就自动开始消耗 GPU 资源。

### 5.2 W&B 要求
所有正式训练必须启用 Weights & Biases，不得静默关闭 W&B。
如果 W&B 无法正常连接，应停止正式训练并报告问题，不得自动切换成不记录模式。
---

## 6. 实验目录与说明文档

每个训练目录或实验目录都必须包含独立的说明文档，推荐命名为：

```text
experiment.md
```

实验说明必须在训练启动前创建。训练结束、中断或失败后，需要补充实际运行结果。

### 6.1 基本信息

实验说明必须记录：

- 实验名称；
- 实验目的；
- 创建时间；
- 负责人；
- 基座模型；
- 初始化 checkpoint；
- 代码基准 commit；
- 当前未提交代码或暂存修改的说明；
- 使用的配置文件；
- 完整启动命令；
- 随机种子；
- 输出目录；
- W&B project、group、run name 和 run URL。

### 6.2 数据集

必须记录：

- 数据集名称和版本；
- 数据集路径；
- 使用的子集；
- train/validation/test 划分；
- 样本数量；
- 数据采样策略；
- 相机视角；
- 是否使用状态、语言、动作、光流或其他标签；
- 动作表示与归一化方式。

### 6.3 训练阶段

列出所有训练阶段及执行顺序。每个阶段必须分别记录：

- 阶段名称；
- 阶段目的；
- 初始化来源；
- 加载哪些 checkpoint；
- 训练哪些模块；
- 冻结哪些模块；
- 是否解冻视觉编码器；
- 是否训练视觉 merger/projector；
- 是否训练语言模型；
- 是否训练 Action Expert；
- 是否训练 Query、Slot、Flow 或其他新增模块。

不允许只写“训练模型”或“冻结 backbone”，必须写清楚具体模块名称。

### 6.4 学习率和优化器

每个阶段、每个参数组分别记录：

- 模块名称；
- 是否可训练；
- 峰值学习率；
- 最低学习率；
- weight decay；
- optimizer；
- scheduler；
- warmup steps 或 warmup ratio；
- 是否使用 cosine decay、linear decay 或 constant；
- gradient clipping；
- 是否使用不同模块的学习率倍率。

### 6.5 训练规模

必须记录：

- GPU 型号；
- GPU 数量；
- 每张卡的 batch size；
- gradient accumulation steps；
- 全局 batch size；
- 全局 batch size 的计算方式；
- 训练步数或 epoch 数；
- 每个阶段实际处理的样本数量；
- 混合精度类型；
- 是否使用 gradient checkpointing；
- 验证间隔；
- checkpoint 保存间隔；
- 日志记录间隔；
- early stopping 配置。

全局 batch size 应明确写为：

```text
global_batch_size =
    num_gpus × per_device_batch_size × gradient_accumulation_steps
```

### 6.6 Loss 设计

每个训练阶段必须记录：

- 使用了哪些 loss；
- 每个 loss 的数学含义；
- 每个 loss 监督哪个输出或模块；
- 每个 loss 的权重；
- loss 是否始终启用；
- loss 对应的配置开关；
- loss 是否参与反向传播；
- 总 loss 的计算公式。

例如：

```text
total_loss =
    λ_fm × flow_matching_loss
    + λ_ar × autoregressive_loss
    + λ_slot × slot_loss
```

关闭某项 loss 时，应确认其权重为零或不进入计算图。

### 6.7 图像处理

训练和评估必须分别记录图像处理流程，包括：

- 原始图像分辨率；
- 使用哪些相机图像；
- 是否 resize；
- resize 后的宽度和高度；
- 是否保持长宽比；
- resize 插值方式；
- 是否 crop、pad 或 letterbox；
- crop 的方式和尺寸；
- 是否进行数据增强；
- 是否归一化；
- 使用的 mean 和 std；
- 多视角图像如何排列或合并；
- 训练与评估的图像处理是否一致。

不得只写“使用默认 image processor”，必须记录最终输入模型的实际分辨率。

### 6.8 动作配置

必须记录：

- action dimension；
- action 表示方式；
- action normalization；
- action chunk；
- action horizon；
- prediction horizon；
- execution horizon；
- 每次推理实际执行多少步；
- 控制频率或数据 FPS；
- 动作 padding 和 mask 方式；
- 末端执行器动作如何编码。

### 6.9 训练完成后的补充

训练结束、中断或失败后，必须补充：

- 实际开始和结束时间；
- 实际完成步数；
- 最终 checkpoint；
- 最低训练 loss；
- 最佳验证指标；
- 对应的 W&B run；
- 是否发生 NaN、OOM 或训练中断；
- 是否从 checkpoint 恢复；
- 与计划配置是否存在偏差；
- 后续评估结果及评估配置。

---

## 7. 评估配置要求

每个评估结果必须能够追溯到：

- 使用的 checkpoint；
- 对应的训练实验；
- 评估数据集与任务集合；
- 随机种子；
- rollout 数量；
- 图像原始尺寸；
- 是否 resize；
- resize 后的实际尺寸；
- crop、pad、归一化等处理；
- action chunk；
- execution horizon；
- 控制频率；
- 成功率计算方式；
- 评估脚本和完整命令。

训练与评估的图像处理不一致时，必须在实验说明中突出记录并解释原因。

---

## 8. 修改完成后的检查

每次任务完成前至少检查：

1. 新增功能是否具有开关；
2. 关闭开关后是否保持原有行为；
3. 是否复用了仓库已有实现；
4. `reference.md` 是否已同步更新；
5. 配置文件和命令行参数是否一致；
6. 是否补充了必要测试；
7. 是否运行了与修改范围相匹配的验证；
8. 是否意外修改了无关文件；
9. 是否只暂存了本次任务产生的代码和文档；
10. 是否存在未被忽略的 checkpoint、日志或数据文件；
11. 是否没有创建 commit；
12. 最终回复中是否说明：
    - 修改了什么；
    - 验证了什么；
    - 哪些文件已进入暂存区；
    - 是否存在未解决的问题或未运行的验证。