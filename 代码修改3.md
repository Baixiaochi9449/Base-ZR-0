请基于当前工作树中的 ZR-0 + Difference Query 实现，做最小改动，加入可插拔的第一版 Optical Flow Auxiliary Head，并贯通三阶段训练和跨阶段 checkpoint 初始化。

不要重写现有 Qwen3-VL、Difference Query、[C,Q,T,P] attention mask、AR loss、Action Expert 或 Flow Matching 主干。

一、先审计，不要立即编码

1. 检查 git status、现有 Difference Query、loss_type、optimizer、checkpoint、resume、dataset registry 和训练入口。
2. 只读检查光流数据：

/opt/data/private/lq/datasets/lerobot/libero/stage06_flow/libero_delta10

3. 检查 manifest、stage06_config、若干 HDF5、quality/status，并确认：
   - flow 对应的 camera key；
   - episode_id、frame_index 与当前 LIBERO 数据如何对齐；
   - 生成光流时的图像预处理与当前训练图像是否一致；
   - 哪些 episode/frame 有标签，哪些没有；
   - 缺失、重复、损坏和 delta 不足 10 的情况；
   - flow、valid_mask、actual_delta_frames、label_source、valid_fraction 和幅值分位数。
4. 将结果写入 docs/optical_flow_data_audit.md。
5. 如果无法可靠确认 camera 或索引映射，停止并报告证据，不允许猜测。
6. 审计后给出简要实施计划，然后继续实现；仅遇到真实阻塞时再询问。

二、三阶段训练语义

增加一个清晰的 canonical training_stage，名称可适应仓库现有风格，但语义必须是：

1. stage1_ar
   - 只有 AR loss；
   - 训练 VLM + Difference Query；
   - 不运行 Optical Flow Head、Slot Head 或 Action Expert；
   - 总损失：
     L = lambda_AR * L_AR

2. stage2_aux
   - 目标是 Slot loss + Optical Flow loss；
   - 不计算 AR loss；
   - 不运行 Action Expert，不计算 FM loss；
   - 总损失：
     L = lambda_slot * L_slot + lambda_OF * L_OF
   - 当前 Slot 尚未实现，因此当前允许以 dense Optical Flow 单独运行 stage2 smoke/预训练；
   - 这种运行必须明确记录为 “stage2 OF-only / Slot not implemented”，不能冒充完整第二阶段。

3. stage3_joint
   - 最终目标：
     L = lambda_AR * L_AR
       + lambda_slot * L_slot
       + lambda_OF * L_OF
       + lambda_FM * L_FM
   - 当前 Slot 未实现时，允许临时运行 AR + OF + FM；
   - 必须在日志和实验 manifest 中标记 slot_loss_computed=false，不能伪造 Slot loss=0 并宣称已经完成四损失联合训练。

所有 loss weight 均由外部传入，不能硬编码。激活的 loss 必须有显式正权重。

如果不传 training_stage，旧训练命令和 loss_type 行为必须保持不变。若同时传入相互冲突的 training_stage 和旧 loss_type，直接报错，不能静默选择其中之一。

三、Slot 接口预留

增加轻量、可替换的 Slot auxiliary registry/config 接口：

slot_aux_type:
- none
- 未来可注册 structured_slots_v1

本次不要实现假的 Slot Head，也不要设计 Slot 标签和 Slot loss。

要求：

- slot_aux_type=none 时不实例化模块、不进入 optimizer。
- 传入未实现的 slot type 时明确抛出 NotImplementedError。
- loss 输出包含 slot_loss_computed=false。
- 接口设计应允许以后从指定 Query 子集计算 Slot loss，而不修改主训练框架。

四、第一阶段 checkpoint 初始化第二阶段

必须区分两种加载语义：

1. init_from_checkpoint
   - 用于跨阶段初始化；
   - 加载模型权重；
   - optimizer、scheduler 和 global step 全部重新开始。

2. resume_from_checkpoint
   - 用于同一阶段断点续训；
   - 恢复模型、optimizer、scheduler、scaler、sampler/RNG 和 global step。

stage1_ar -> stage2_aux 时：

- 精确恢复 Qwen/VLM 和 Difference Query；
- Query 开关及总 Query 数以 checkpoint 为准；
- 显式配置冲突时报错；
- stage1 checkpoint 没有 Optical Flow Head 是合法情况；
- 根据显式配置新建 dense_regression_v1，并在隔离 RNG context 中随机初始化；
- 不加载、不初始化、不 prepare、不 forward Action Expert；
- stage2 optimizer 只包含 VLM、Difference Query、Optical Flow Head，以及未来真正启用的 Slot Head；
- 保存初始化前后 VLM/Query checksum 或逐参数比较，证明它们被精确恢复；
- 证明新增 Flow Head 没有改变全局 RNG、数据顺序或其他模块初始化。

stage2_aux -> stage3_joint 时：

- 恢复 VLM、Difference Query、Optical Flow Head 和未来的 Slot Head；
- Action Expert 默认按照原始 ZR-0 流程和固定 seed 随机初始化；
- 不得意外恢复 stage1/stage2 checkpoint 中可能存在但未训练的旧 Action Expert；
- 只有显式提供独立有效的 Action Expert checkpoint 时才允许加载；
- stage3 optimizer 包含所有实际启用且应训练的模块。

旧 checkpoint 直接用于推理时仍默认关闭 Optical Flow Head。只有跨阶段初始化显式启用 head 时，才允许为旧 checkpoint 新建 head。

五、Optical Flow Auxiliary 接口

使用 registry/factory：

optical_flow_aux_type:
- none
- dense_regression_v1

未来可增加：
- vq_token_ce
- latent_action_v2

配置至少包括：

- optical_flow_aux_type
- num_flow_queries
- optical_flow_loss_weight
- optical_flow_data_root
- flow_camera_key
- flow_delta_frames，默认 10
- flow_target_resolution，默认 56
- flow_head_hidden_dim，默认 256
- flow_head_num_heads，默认 8
- flow_head_num_layers，默认 2
- flow_grid_size，默认 14
- flow_cell_min_valid_fraction，默认 0.5
- flow_motion_threshold_px
- flow_motion_loss_weight，默认 1.0
- flow_loss_epsilon，默认 1e-3

约束：

- Difference Query 关闭但 Flow Head 开启：立即报错。
- Query 开启但 Flow Head 关闭：合法。
- 首次开启 Flow Head 必须显式指定 num_flow_queries 和 optical_flow_loss_weight。
- 1 <= num_flow_queries <= num_difference_queries。
- Flow Query 固定取最后 N 个：
  query_states[:, -num_flow_queries:, :]
- Action Expert仍读取全部 Difference Query，不是只读取 Flow Query。
- Flow Head 关闭时，不读取 HDF5，也不改变原 DataLoader 和模型路径。

六、dense_regression_v1 架构

输入：

[B, num_flow_queries, H_vlm]

结构：

1. LayerNorm。
2. Linear：H_vlm -> flow_head_hidden_dim。
3. 创建 flow_grid_size² 个可学习二维 grid tokens。
4. grid tokens 作为 Query，Flow Query 作为 Key/Value。
5. 使用可配置的 cross-attention + residual + LayerNorm + MLP block。
6. reshape 为二维低分辨率 feature map。
7. 使用 bilinear upsample + 3×3 Conv + normalization + GELU 上采样。
8. 输出：
   [B,2,flow_target_resolution,flow_target_resolution]

限制：

- Head 只能读取最后 N 个 Flow Query。
- 不能读取原始图像/text token、state、action、future RGB 或 AR target。
- 光流标签只能进入 loss。
- 输出不使用 sigmoid/tanh。
- 最后卷积 bias=0，weight 使用很小但非零的初始化，例如 std=1e-3。
- 不要严格零初始化最终权重，否则第一次 backward 无法把 OF 梯度传给 Query/VLM。
- 实现后使用 p.numel() 报告 Head 的精确总参数量、可训练参数量和各子模块参数量。
- 不同 num_flow_queries 下 Head 参数量应保持不变。

七、部分样本没有光流标签

Dataset/collator 必须显式返回：

- flow_supervision_available: bool
- flow_target
- flow_valid_mask
- flow_actual_delta_frames
- flow_label_source
- 用于审计的 episode_id/frame_index

判定规则：

- episode/frame 不在光流覆盖范围内：available=false。
- actual_delta_frames != 10：available=false。
- label_source != 1：available=false。
- 没有有效像素：available=false。
- manifest 声明标签存在但文件损坏、字段缺失或 frame 映射不一致：这是数据错误，必须报错，不能伪装成无标签。
- 缺失标签绝不能用全零光流冒充监督。

混合 batch 中：

- 有标签样本正常计算 OF loss。
- 无标签样本对 OF loss 的分子和分母贡献均为零。
- 无标签样本不会单独产生 OF 梯度；共享参数仍可能因同 batch 的有标签样本或其他 loss 被更新，这是正确行为。
- OF loss 按有效样本平均，不能按原 batch size 平均，否则标签覆盖率会改变 loss 尺度。

分布式训练中：

- 跨 rank 聚合有效样本数和 loss 分子，保证全局均值正确。
- 某个 rank 局部没有有效流、但其他 rank 有时，该 rank 使用 graph-connected zero 参与 DDP，避免 collective/unused-parameter 错误。
- 如果整个全局 batch 在 stage2 中既没有有效 Flow supervision，也没有未来有效 Slot supervision，则所有 rank 同步跳过 backward、optimizer.step、scheduler.step 和 global_step 增长。
- 不能对这种全局空监督 batch 执行 AdamW，否则即使 loss=0，weight decay 也可能改变参数。
- stage3 仍有 AR/FM 时不能跳过整步，只令 OF 项为零。

记录：

- batch/sample 标签覆盖率；
- skipped global-empty batch 数；
- 每个数据源的 flow coverage；
- eligible sample/pixel count。

不要默认过滤没有光流的样本。可以预留显式 flow_labeled_only sampler 供 OF-only 调试，但默认保持原数据分布。

八、光流读取

Reader 必须：

- 通过 manifest/config 建立 `(episode_id, frame_index)` 映射；
- 校验 HDF5 内实际 frame_index，不能假设 row index 等于 frame index；
- 每个 DataLoader worker 独立懒加载；
- 使用小型 LRU HDF5 handle 缓存；
- worker 序列化时清理 handle，并正确关闭文件；
- 不在主进程打开 HDF5 后传给 worker；
- h5py 只在启用光流数据时成为运行时依赖；
- 不跨 episode；
- 默认允许数据集中部分 episode/frame 没有光流标签。

必须锁定 flow_camera_key。若图像有随机 crop/flip/rotation，图像和 flow 必须同步变换，翻转时修正对应 flow 分量符号；无法保证时，第一版关闭随机几何增强并记录。

九、Target 与 loss

HDF5 保存的是按源图像宽高归一化的位移。下采样到 56×56 时：

- 不要把 flow 除以 4；
- 不要按每张图最大幅值重新归一化；
- 使用 mask-aware area downsampling；
- 无效位置的零值不是真实静止监督。

计算：

target_small =
    area(flow * valid_mask)
    / clamp(area(valid_mask))

cell_valid_fraction = area(valid_mask)

small_valid_mask =
    cell_valid_fraction >= flow_cell_min_valid_fraction

Loss 始终用 float32。

转换到 224 标注画布的等效像素误差：

dx_px = (pred_x - target_x) * 223
dy_px = (pred_y - target_y) * 223

robust_epe =
    sqrt(dx_px² + dy_px² + epsilon²) - epsilon

L_all = 每样本 masked mean robust_epe，再对有效样本平均

motion_mask =
    valid_mask
    & (GT flow magnitude in pixels >= flow_motion_threshold_px)

L_motion 同样先按样本平均。

L_OF = L_all + flow_motion_loss_weight * L_motion

无 motion pixel 的样本不参与 L_motion，不能产生 NaN。

记录：

- flow_epe_px_all
- flow_epe_px_motion
- zero_flow_epe_px_all
- zero_flow_epe_px_motion
- flow_valid_pixel_fraction
- flow_motion_pixel_fraction
- flow_eligible_sample_count

十、推理边界

默认 direct-action/get_action/server：

- 不读取光流 HDF5；
- 不运行 Optical Flow Head；
- 不预测光流；
- 不增加闭环推理开销。

允许显式 flow validation API 在 eval 中运行 Head。

保持现有 [C,Q,T,P] mask。测试必须证明：

- 修改 teacher-forced T 不改变 Query hidden 和 flow prediction；
- 修改 C 会改变 flow prediction；
- 修改 flow target 只改变 OF loss，不改变 Query、AR logits 或 action prediction；
- Action Expert 读取全部 Query，不读取 flow prediction；
- Flow Head 精确读取最后 N 个 Query。

十一、Checkpoint

按仓库现有 checkpoint 结构保存 Optical Flow 配置和权重；可使用 sidecar：

- optical_flow_aux_config.json
- optical_flow_aux.safetensors

要求：

- 旧 checkpoint 无 Flow 配置时默认关闭。
- init_from stage1 + 显式启用 Flow Head时允许随机初始化。
- stage2/stage3 checkpoint 自动恢复 Head 类型、结构、N 和权重。
- CLI 与 checkpoint 显式冲突时报错。
- 配置存在但权重缺失、权重存在但配置缺失、shape 不匹配均报错。
- resume 必须恢复 Head、optimizer、scheduler、RNG 和 global step。
- direct-action 推理加载带 Head 的 checkpoint 时不能要求 optical_flow_data_root。

十二、测试与验证

至少覆盖：

1. Head 关闭时旧 Query-off/Query-on 路径回归不变。
2. 三种 training_stage 的实际 forward 模块和总损失正确。
3. stage1 不运行 Action Expert/Flow/Slot。
4. stage2 不运行 AR/Action Expert。
5. stage3 正确组合所有当前已实现的 loss。
6. 未实现 Slot 不被伪造成有效 loss。
7. stage1 -> stage2 精确恢复 VLM/Query，新初始化 Flow Head。
8. stage2 -> stage3 恢复 VLM/Query/Flow Head，Action Expert按固定 seed 初始化。
9. init 与 resume 语义不同且正确。
10. N=1、4、12、全部 Query均可工作。
11. Head 参数量不随 num_flow_queries 改变。
12. 部分样本无 flow 时只屏蔽对应样本。
13. 全局空监督 stage2 batch 完整跳过 optimizer/scheduler/global step，参数包括 weight decay 下保持不变。
14. 局部空、全局非空的多 rank batch 可正常 backward。
15. loss 的跨 rank 归一化正确。
16. reader 映射、多 worker、损坏标签和未覆盖样本处理正确。
17. OF loss 对 Head、Flow Query和VLM产生有限非零梯度。
18. flow-only 时 Action Expert不进入 optimizer、无参数变化。
19. 默认推理完全不运行 Head。
20. checkpoint 保存加载后预测和 loss 一致。
21. 新 Head 初始化不改变 baseline RNG和Action Expert初始化。

验证顺序：

- 数据审计；
- 单元测试；
- 合成数据 tiny forward/backward；
- 真实 HDF5 少量读取；
- 16～64 个有标签样本的 OF-only overfit；
- 验证 EPE 下降且优于 zero-flow baseline；
- 资源允许时运行真实 Qwen3-VL-2B 的 1～2 step smoke；
- 不启动正式训练或完整 LIBERO rollout；
- 不抢占已有 GPU 任务。

提供启动模板：

1. stage1_ar（仅作为已有训练语义参考，不重新正式训练）。
2. 从用户现有 stage1 checkpoint 初始化 stage2_aux OF-only。
3. 恢复 stage2 checkpoint 继续训练。
4. 从 stage2 checkpoint 初始化 stage3_joint。
5. Difference Query + FM baseline。
6. Difference Query + FM + OF 对照。
7. 梯度范数审计命令，用于以后选择 lambda_OF。

不要替用户决定正式 lambda_OF、训练步数或 Slot loss 权重。

十三、文档与工作树

文档明确：

- 第一版是训练期连续光流辅助回归，不是 FlowVLA VQ token 生成。
- MegaFlow 是 pseudo-label。
- valid_mask 不是遮挡/置信度。
- Flow Query 可解码光流，不等于运动信息只存在于这些 Query。
- 当前 Query 组内双向注意，因此其他 Query 可间接影响 Flow Query。
- 当前无 Slot 实现时，stage2/stage3 都是不完整的临时版本。
- 推理时跳过 Flow Head。

保留所有现有未提交修改，不覆盖无关文件，不处理用户笔记，不创建 commit，不启动正式训练。

最终报告：

- 修改文件；
- 数据审计结论；
- 精确 Head 参数量；
- 各阶段可训练参数集合；
- checkpoint 初始化与恢复验证；
- 测试和 smoke 的实际结果；
- 未验证项和剩余风险。