你负责在当前 ZR-0 项目上完成三阶段预训练配置接入和有界验证。请先检查实际代码、模型和数据，再复用已有能力；仅对缺失能力做最小修改，不重构已经审核通过的 Query、Slot、Optical Flow 和训练恢复体系。不要只返回计划，请在以下边界内完成实现、测试和验证。

## 一、执行边界与环境

- 仓库为 `/opt/data/private/lq/ZR-0`，Python 为 `/opt/data/private/lq/miniconda3/envs/ZR-0/bin/python`。先读取仓库约束、当前训练说明、最新修复记录及 launcher，核验解释器、依赖和实际模块来源。若无法访问真实环境，报告阻塞，不在其他目录的代码副本上冒充完成。
- 本次授权：必要的最小代码/配置修改、独立数据索引和统计产物、回归测试，以及真实四卡三阶段各 100 次有效 optimizer 更新。正式 20k/5k/150k 训练仅生成配置与命令，不得执行。
- 保留已有未暂存和暂存修改，不自动暂存或 commit，不覆盖 base、历史 checkpoint、原始数据或历史 sidecar。新产物使用独立目录；不得关闭完整性检查换取通过。
- 不终止他人 GPU 任务，不擅自升级或重装环境。四卡资源不足时完成可执行的检查并报告，不能以 CPU/tiny 结果替代完整模型 GPU 验证。

## 二、模型初始化与阶段转换

唯一基础模型来源是 `/opt/data/private/lq/models/ZR-0`。取消原始 Qwen 权重初始化以及 Action Expert 随机初始化。

| 组件 | Stage 1 | Stage 2 | Stage 3 |
|---|---|---|---|
| VLM，包含视觉模块与文本输出层 | 从 base 加载并更新 | 继承 Stage 1，冻结 | 继承 Stage 2，更新 |
| Difference Query | 随机初始化并更新 | 继承 Stage 1，更新 | 继承 Stage 2，更新 |
| Slot Head | 不构造 | 随机初始化并更新 | 继承 Stage 2，更新 |
| Optical Flow Head | 不构造 | 随机初始化并更新 | 继承 Stage 2，更新 |
| Action Expert，包含 state/action 编解码器 | 不构造 | 不构造 | 从 base 加载预训练权重并更新 |

Stage 3 必须按组件加载，不能重新加载整个 base 覆盖 Stage 2 的 VLM、Query 和辅助 Heads。跨阶段初始化重建 optimizer、scheduler 和训练计数；同阶段 resume 恢复完整状态。记录各组件来源、参数量、可训练状态及加载校验结果。

从 base 读取真实配置、processor 和 hidden size，不硬编码为原始 Qwen 配置。严格检查权重完整性、条件维度以及 `action_horizon=10` 的兼容性；禁止通过忽略 shape mismatch、丢弃权重或随机重建层冒充完整加载。若 base 已含 Query/辅助配置，或需要结构性权重转换，先报告，不静默覆盖。首次更新前按一致 dtype 比对组件权重与其来源。

## 三、四数据集、筛选与归一化

使用以下四集，不追加 LIBERO：DROID full-95658 的实际路径从现有 registry 核验并记录，禁止继续使用 partial9465；另外三集分别为 `/opt/data/private/lq/datasets/molmoact_dataset_household-v3_stage05`、`/opt/data/private/lq/datasets/molmoact_dataset_tabletop-v3_stage05`、`/opt/data/private/lq/datasets/RH20T-v30_stage05`。参考总帧数依次为 27,630,375、794,199、310,743、3,898,056；实际身份或计数不符时解释差异，不能默默替换数据范围。

Stage 1/2 仅使用“有效 train_data 且有效 slot_data”的交集。有效性必须沿用已审核的解析、锚点和逐任务 mask 合同；非空 JSON、沿用的非锚点标签不等于有效 Slot 监督。Stage 1 只计算 AR，不把 slot_data 放进输入。Stage 2 不要求光流存在。Stage 3 不按辅助标签过滤，保留四集全部训练样本，各目标独立判断监督。

首版按各阶段筛选后的帧数比例混合，样本等权，不额外过采样小数据集。核验生产 sampler 的实际语义，记录各集有效样本数、混合概率和实际曝光量，不能把数据集等权与样本等权混为一谈。14,510,521 条非空专家文本仅是参考计数，不能当成 Stage 1/2 交集大小。

每集使用自身 Flow manifest，核验源/合并 episode 映射、frame index、相机、单位、FPS、delta 和有效性。保留按需读取及 worker 句柄管理，不能启动时遍历打开全部 HDF5。缺失标注可关闭监督；manifest 声明存在但损坏的文件必须报错，不伪装成缺失。full DROID 的 sidecar/统计须独立核验，不能继承 partial 的验证结论。

归一化沿用现有实现，但各数据集分别使用与实际 state/action 表示匹配的训练集统计量。先核验动作类型、单位、坐标系、维度、夹爪语义以及统计来源；现有统计合格则复用，否则在独立目录补算。统计须对应实际动作转换，排除 padding、无效值及验证集，不按 batch/episode 临时计算，也不因辅助标签筛选随意改变动作尺度。数据内部动作语义不同则进一步分组，不能仅靠数值缩放掩盖语义不一致。

Stage 1 不需要动作统计；Stage 2 不需要 Action Expert 的动作归一化，但保留 Slot 自身统计。Stage 3 在归一化动作空间计算 FM，部署使用目标机器人对应统计反归一化。Flow 保留已有归一化位移单位，不套用动作统计。验证常量维、截断、padding 和归一化往返；不把带截断的转换声明为完全可逆。

## 四、模型约束与固定超参

使用 `num_difference_queries=32`、`num_flow_queries=16`，最后 16 个为 Flow 组，前 16 个按已有 Slot 分组规则分配；从 Stage 1 起保留一致角色配置。辅助 Head 使用 `structured_slots_v1` 和 `dense_regression_v1`，仍支持独立开关。分组不代表 Query 间梯度隔离。

每条样本仅输入两路当前 RGB 和允许的任务上下文。按数据集固定相机映射，必须保留监督标签对应视角；不能随意复制图像凑两路。图像几何预处理须与 bbox、接触点、光流一致，首版不新增几何增强。记录实际相机键、图像尺寸、文本长度上限和监督 token 截断率；未来图像、train_data、slot_data、光流及其他未来标签不能进入 C。

保留 `[C,Q,T,P]` mask、mRoPE、视觉/DeepStack 注入和 final RMSNorm：Q 读取 C 与双向 Q，不见 T；T 读取 Q 与过去 T，不直接读取 C；动作专家的 VLM 条件仅为全部 32 个 Query。保留动作专家正常的 state、噪声动作及时间步输入。AR 使用 teacher forcing，未支持的 `.generate()` 继续明确拒绝。Stage 2 冻结 VLM 参数但不能切断 Query 经 VLM 的反传。

共同配置：四卡；全局 batch 128；初始每卡 micro-batch 16、GAS 2；BF16；ZeRO-2；SDPA；AdamW，betas=(0.9,0.95)、epsilon=1e-6、weight_decay=0.01；cosine、warmup_ratio=0.08、min_lr_ratio=0.1；gradient_clip=1.0；seed=42；window_size=1；action_horizon=10；state/action padding=64；不使用 LoRA。显存不足时可降低 micro-batch 并相应增加 GAS，保持全局 batch 128，必须记录调整。未列参数先沿用已审核配置，并在执行前完整展开，不能隐式改变。

| 阶段 | 总损失 | 峰值 LR | 正式更新次数 | 正式 warmup | 正式保存间隔 |
|---|---|---:|---:|---:|---:|
| Stage 1 | AR | 1e-5 | 20,000 | 1,600 | 1,000 |
| Stage 2 | Slot + OpticalFlow | 2e-5 | 5,000 | 400 | 1,000 |
| Stage 3 | AR + Slot + OpticalFlow + 5×FM | 1e-5 | 150,000 | 12,000 | 2,000 |

所有 loss 外层权重从外部传入，光流权重固定为 1，不使用此前撤回的 100。Slot 子项和 Flow 内部系数沿用已审核配置并记录，不自动调整。保持各目标按其有效监督单位进行正确的跨 rank/GAS 归约；缺失目标不重新配平其他目标权重。无标签的损失贡献为零，共享参数仍可由其他目标更新；独立 Head 整个窗口无活动监督时，检查不会因零梯度、动量或 weight decay 意外更新。全局无活动目标或 AMP 跳步不推进有效更新计数和 scheduler，数值恰为零但有有效监督的 loss 不能误判为空窗口。

## 五、测试与三阶段各 100 步验证

先完成相关回归、真实四集样本与 collator 检查、组件加载校验、CLI/launcher dry-run。覆盖 Query-off、Stage 1 兼容、辅助 Head 独立关闭、32/16 分区、horizon 10、缺失监督和归一化路由。不删除旧测试或弱化断言；历史“596 passed”不能替代本轮执行证据。

四卡空闲且前置检查通过后，使用真实完整 base、生产 loader/collator/loss 和 ZeRO-2，顺序运行 Stage 1→Stage 2→Stage 3，每阶段仅 100 次成功 optimizer 更新。每阶段 scheduler 总长度为 100、warmup 为 8，不使用正式长 warmup。Stage 2 使用本轮 Stage 1 的第 100 步模型，Stage 3 使用本轮 Stage 2 模型及 base Action Expert。

每阶段第 50 步保存真实完整 checkpoint，结束本阶段进程并在新进程同阶段 resume 至第 100 步，再保存。核验模型、optimizer、scheduler、RNG、sampler/数据游标及后续输入；不能用 `_TinyEngine`、固定 samples.pt 重放或仅成功加载代替生产恢复。输入身份要求精确一致，模型数值比较预先声明容差，出现差异不能临时放宽。未覆盖真实 epoch 边界或其他恢复场景时如实列为缺口。

监控各阶段 loss 有限性、raw/weighted loss、监督覆盖、模块更新、梯度、LR、吞吐和峰值显存；抽样诊断各损失对共享 Query 的梯度，不要求 loss 单调下降。每阶段第 100 次有效更新后必须退出；连续 20 个累积窗口无有效更新时停止并报告，防止无限遍历。异常不自动扩大训练预算，不启动无人值守重试。

## 六、交付与停止条件

交付最小修改说明、实际展开配置与命令、测试结果、组件权重来源、四集索引/统计身份、两相机映射、每阶段第 50/100 步 checkpoint 路径、真实恢复证据、loss/梯度/资源摘要及未验证事项。每个验证目录创建 experiment.md，保留既有日志要求，不擅自上传原始样本。正式命令与验证命令分开，禁止自动串接正式训练。

文档明确区分用户选择、现有扩展、公开脚本及论文设置：本方案 warmup 8% 和 epsilon=1e-6 沿用公开脚本；论文为 5% 和 1e-8。分阶段 LR 属于本项目建议，整套方案不是 ZR-0 原始预训练协议的完全复现。

完成三个阶段各 100 步验证后停止，汇报结果并等待我的明确指令。不得自动继续 20k/5k/150k，不将验证 checkpoint 自动作为正式训练起点，不创建 commit。若发现超出最小修改范围的结构、动作语义或数据合同冲突，给出证据和需要我决定的事项，不自行改变实验目标。