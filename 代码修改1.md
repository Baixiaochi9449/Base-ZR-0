请基于当前 ZR-0 仓库，以最小改动实现可开关的 Difference Query，并完成代码验证。修改前先阅读并说明当前 VLM 输入、VLM hidden states、动作专家 cross-attention mask、训练、推理和 checkpoint 加载的完整数据流，再开始修改。
一、实验目标
本阶段只检验一个问题：
将视觉语言上下文压缩到固定数量的 learnable Difference Query 后，动作专家只通过这些 Query 获取 VLM 条件，LIBERO 成功率会如何变化？
本阶段“动作专家只读取 Difference Query”特指：
- 动作专家对 VLM 的 cross-attention 只能读取 Difference Query hidden states。
- 保留 ZR-0 原有的 proprioception/state encoder 路径。
- 不修改 Flow Matching、DiT、state encoder、action encoder 和 action decoder 的结构。
二、配置接口
增加并贯穿训练、推理、评估和 checkpoint 加载流程的参数：
- use_difference_query: bool = False
- num_difference_queries: int = 32
命令行增加：
- --use_difference_query
- --num_difference_queries 32
当开关关闭时，必须完全执行原始 ZR-0 路径，输入、attention mask、动作专家条件、训练和推理行为均保持不变。
三、Difference Query
在 VLM hidden size 上定义彼此独立的可学习参数：
- 即使 VLM 冻结，Difference Query 仍应可单独训练。
- 确认优化器包含 Difference Query 参数，并验证其能从 action loss 获得非零梯度。
优先通过 inputs_embeds 插入 learnable Query，不要无必要地扩充 tokenizer 词表。
四、序列位置
把序列明确划分为：
- C：视觉、任务指令、generation prompt 等条件 token
- Q：Difference Query
- T：可选的自回归目标文本
- P：padding
重组后的顺序必须是：
[C, Q, T, P]
在没有目标文本的 LIBERO 推理中，Query 就是最后一段有效输入；如果训练样本仍包含 assistant/ECoT/离散动作文本，Query 必须插在这些目标文本之前，不能放在整个序列末尾。
五、VLM 内部 attention mask
开启 Difference Query 后，构造显式的 block attention mask：
暂时无法在飞书文档外展示此内容
同时满足：
- padding token 不能作为任何有效 token 的 key/value。
- C 的原有注意力关系不得改变。
- 所有 Query 可以相互双向注意。
- Query 可以读取所有有效条件 token。
- Query 不能读取任何目标文本，避免标签泄漏。
- 自回归文本不能直接读取 C，只能读取全部 Q，并因果读取此前文本 token。
日志中打印实际使用的 attention backend。
六、动作专家条件
开启 Difference Query 后，从 VLM 最后一层 hidden states 中按每个样本的 Query 位置 gather：
- backbone_embeddings = query_hidden_states
- shape 必须严格为 [B, num_difference_queries, hidden_size]
- action_expert_cross_attn_mask 为 [B, num_difference_queries] 的全 True mask
不要继续把 C、T 或 padding hidden states传入动作专家。请在动作专家调用边界增加断言，确保其 VLM 条件序列长度等于 num_difference_queries。
关闭开关时继续使用 ZR-0 原来的全部 hidden states和原始 cross-attention mask。
训练和推理必须调用同一套 Query 插入、mask 构造和 Query hidden-state 提取逻辑，避免两条路径行为不一致。
七、保存与加载
当前 ZR-0 只保存内部 VLM 和动作专家，因此必须额外处理 Difference Query：
- 保存 Query 权重、开关状态和 Query 数量。
- 加载带 Query 的 checkpoint 时自动恢复配置与权重。
- 从原始 ZR-0 checkpoint 启用 Query 时，若不存在 Query 权重，则随机初始化并打印明确提示。
- 原始 checkpoint 在关闭 Query 时必须保持兼容。
- Query 数量与 checkpoint 不一致时明确报错，不允许静默截断或补齐。
- 检查训练恢复、server 推理和 from_pretrained 等所有模型构造入口。
八、验证要求
至少完成以下测试：
1. use_difference_query=False 时，与修改前固定输入的输出和 mask 保持一致。
2. 用小型 toy sequence 验证上述 C/Q/T block mask 的每一个区域。
3. 改变 T 的内容不能改变 Q hidden states；改变 C 应能改变 Q hidden states。
4. 动作专家收到的 VLM 条件严格为 [B, Nq, H]。
5. action loss 能向 Difference Query 传播非零梯度。
6. 分别测试 Nq=8、32、64。
7. 保存再加载后，Query 权重和输出保持一致。
8. 完成一次小 batch 的训练 forward/backward 和一次 direct-action 推理。
9. 检查训练与推理期间无 NaN/Inf，且 attention mask 的 dtype、device 和语义正确。
九、实验公平性
LIBERO 对照实验除 Difference Query 开关外，保持以下条件完全一致：
- 初始化 checkpoint
- 数据集和数据顺序
- seed
- batch size
- epochs/steps
- optimizer、学习率和调度器
- action horizon
- denoising steps
- 是否全量微调
- 评估任务、episode 数和 replan steps
先复现关闭 Query 的原始成功率，再评估开启 Query 的结果。报告每个 LIBERO suite 的成功率及总体平均值，不只报告最佳 checkpoint。
十、交付内容
完成后请提供：
- 修改文件列表及每处修改的作用
- 开关关闭和开启时的数据流
- block mask 的可视化示例
- Difference Query 参数量
- 训练与评估命令
- 所有测试结果
- 已知兼容性或性能影响
- 尚未实现的内容
不要顺带重构无关代码